fastapi==0.111.0  # Pin core framework; consider coordinated upgrade to >=0.116 with Starlette after validating tests
uvicorn[standard]==0.30.1  # Runtime server; upgrading to 0.35.x deferred until FastAPI bump cycle
pydantic==2.8.2  # Data validation; hold <2.12 until full regression pass
python-dotenv==1.1.1  # Bumped (minor fixes)
requests==2.32.5  # Bumped (bug/security fixes)
jmespath==1.0.1
fastjsonschema==2.21.2  # Bumped minor
prometheus-client==0.22.1  # Bumped to latest stable (metric exposition)
pynacl==1.5.0
dnspython==2.7.0
idna==3.7  # Safe to leave; 3.10 available but not required
openai==0.28.0  # Legacy SDK pinned; TODO: migrate to >=1.x new API surface
opentelemetry-api==1.25.0  # Pinned; coordinated OTEL stack upgrade deferred
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-requests==0.46b0
opentelemetry-exporter-otlp==1.25.0
